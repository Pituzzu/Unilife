
import { User, Circle, Note, Announcement } from './types';

export const EMAIL_DOMAIN = '@unikorestudent.it';

// I dati Mock non sono più necessari per il funzionamento ma restano come riferimento tipi
export const MOCK_USERS: User[] = [];
export const MOCK_CIRCLES: Circle[] = [];
export const MOCK_NOTES: Note[] = [];
export const MOCK_ANNOUNCEMENTS: Announcement[] = [];
